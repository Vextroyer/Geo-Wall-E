//Sample program to save functionalities to test.

// fib(n) = if n <= 1 then 1 else fib(n - 1) + fib(n - 2);

// print fib(0);
// print fib(1);
// print fib(2);


f(x) = let eval if 1 then "pepe" else 2; in x;

print f(2);